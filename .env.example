# 复制为 .env 并填入真实值。.env 已被 .gitignore 忽略，禁止提交。
# 注意：VITE_ 前缀变量会内联进前端 bundle，任何访问者都能看到——
# 若用于生产，请改为后端代理转发，或让用户自带 key。
VITE_DS_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
VITE_DS_MODEL=deepseek-chat
VITE_DS_API_BASE_URL=https://api.deepseek.com
