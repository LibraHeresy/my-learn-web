# div 与 span — 块级与内联元素

:::music-analogy
`<div>` 像**小节线**，把内容划分成独立的段落，每个段落独占一行；`<span>` 像**连音线**，在句子内部连接和标记某些词语，不打断句子的流动。
:::

:::explain{title="两种最基本的\"容器\""}
div 和 span 本身不添加任何样式，它们是用来把内容分组的——div 是块级元素（独占一行，就像独奏段落独占舞台中央），span 是内联元素（在文字流中不换行，就像装饰音/加花）。

class 属性可以给它们贴上分类标签（🌐全局属性——所有标签都能用）。class 就像给衣服分类——"这批是夏装""这批是冬装"，同一类衣服可以挂在不同的衣柜里。同一个 class 可以给多个元素用。

具体写法：
- `<div>` — **块级元素**（block），独占一行，像一整个段落
- `<span>` — **内联元素**（inline），在文字流内部，像句子中的标记
```html
<div>我是一个块，占满整行。</div>
<div>我是另一个块，自动换行。</div>
<p>文字中<span>这个部分</span>被span标记了。</p>
```
:::

:::explain{title="什么时候用 div？什么时候用 span？"}
**用 `<div>` 当你要：**
- 创建一个卡片、面板、区块
- 把一组元素包在一起
- 需要块级布局（独占一行）
**用 `<span>` 当你要：**
- 高亮句子中的某个词
- 给行内文字加特殊样式
- 不改动文字流的情况下标记内容
简单记忆：**div 管"块"，span 管"字"。**div 就像衣柜里的抽屉——一格一格各自分区；span 就像在书里用荧光笔画几个字做标记。
**class 和 id 的区别**（两个都是全局属性，但用法完全不同）：
- class = 琴竹分类（硬竹/软竹/反竹）——多个元素可以共用同一个 class 名，CSS 用 .class名 选中
- id = 琴弦唯一编号（C3/D3/E3…）——整个页面同个 id 只能出现一次，CSS 用 #id名 选中

什么时候用 class？给一组相似元素统一样式（比如所有"作曲家名字"都加金色）
什么时候用 id？给独一无二的元素特殊样式（比如页面唯一的"提交按钮"）
:::

:::example{title="看例子"}
下面的代码用 `<div>` 创建了三张音乐卡片，用 `<span>` 在文字中高亮了作曲家名字：
```html
<div class="card">
  <h2><span class="name">巴赫</span></h2>
  <p>时期：<span class="name">巴洛克</span></p>
</div>
```
注意：每个 `<div>` 独占一行，而 `<span>` 只是在行内标记文字。
:::

:::task{title="动手试试 ✨"}
::::step{purpose="练习 `span` 的用法——`span` 是内联元素，在文字流中不换行，只标记其中的一部分。" expected="第三张卡片的作曲家名字和时期也有了高亮样式。"}
给第三张卡片也加上 `<span class="name">` 高亮作曲家名字和时期
::::

::::step{purpose="理解 `div` 的块级特性——**每个 div 独占一行**，新卡片会自动换到下一行。" expected="复制出来的卡片出现在下方新的一行，而不是并排。这就是块级元素的行为。"}
试着复制一张卡片（整个 `<div>` 块），看看它是独占一行还是并排
::::

::::step{purpose="直接对比块级（`div`）和内联（`span`）的差异——`div` 会打断文字流强制换行，`span` 不会。" expected="改成 `<div>` 后，原本流畅的文字在中间断开了。这就是块级 vs 内联最直观的区别。"}
把某个 `<span>` 改成 `<div>`，观察布局变化
::::

:::

:::recap
这一节你学会了两种"容器"——`<div>` 独占一行，用来划分大块区域（像卡片、面板）；`<span>` 不换行，用来标记句子里的某几个字。你还学会了用 `class` 给元素贴标签，同一类元素可以有相同的样式。现在你可以把网页内容分门别类地组织起来了。
:::

:::listen-to
巴赫《勃兰登堡协奏曲》— 不同乐器组（就像 div）各自成段，但独奏乐器在其中穿插对话（就像 span）。
:::

