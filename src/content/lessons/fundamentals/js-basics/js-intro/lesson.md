# 认识 JavaScript — 给页面注入生命

:::analogy
如果 HTML 是房子的骨架、CSS 是装修，那么 JavaScript 就是水电开关——它让页面"活"起来，能点击、能互动、能根据用户的操作做出反应。
:::

:::explain{title="这一章你会学什么"}
这一章会让页面从“能看”变成“能响应”。你会开始接触：
- 变量、函数、条件、循环
- 事件、定时器、DOM 操作
- 数据如何影响页面上的结果

学完这一章，你会第一次真正感受到“我不是只在写页面，我在让页面工作”。
:::

:::explain{title="为什么这章重要"}
JavaScript 决定的是“页面能做什么”。  
在工程语境里，你之后会一直看到这些标准说法：**逻辑**、**状态**、**事件处理**、**DOM 更新**。  
这一章的重点不是把所有语法一次记住，而是建立“输入 → 处理 → 输出结果”的基本思维。
:::

:::hint{title="给第一次接触编程逻辑的你"}
如果你在这章第一次感到抽象，不代表你不适合编程。  
你只是从“看得见的结构和样式”走到了“看不见的规则和流程”。  
先理解例子能不能跑通，再慢慢记名字，比硬背语法有效得多。
:::

:::explain{title="什么是 JavaScript？"}
HTML 决定了页面上"有什么"，CSS 决定了"长什么样"，而 JavaScript（简称 JS）决定了"能做什么"。
有了 JavaScript，网页可以：
- 响应用户的点击和输入
- 动态改变内容和样式
- 播放音乐和动画
- 记住用户的操作
就像电工接通电路让电器从静止变成运转！
:::

:::explain{title="JS 注释：给自己留备忘"}
代码越写越多以后，你需要给代码加一些说明——这段在做什么、为什么要这样写。JS 有**两种注释**：
**单行注释** `//`：
```js
// 这是一条注释，浏览器会忽略它
alert("你好！"); // 行末也可以加注释
```
**多行注释** `/* */`：
```js
/*
 * 这里可以写
 * 多行注释内容
 */
alert("你好！");
```
注释不会被浏览器执行——它们只给"人类"看，包括未来的你自己。
**快捷键：** 选中代码按 `Ctrl + /`（Mac: `Cmd + /`），编辑器自动添加或移除 `//` 注释。这个快捷键在 HTML、CSS、JS 中都通用。
:::

:::explain{title="第一行 JavaScript"}
JavaScript 写在 `<script>` 标签中。先看一个最简单的方法——`alert()`，它会弹出一个提示框：
```html
<script>
  alert("你好，世界！");
</script>
```
打开 JS 选项卡，你可以看到编辑器中已经有了这段代码。预览区会在页面加载时弹出问候。
:::

:::explain{title="console.log — 开发者的耳朵"}
`console.log()` 是 JS 中最常用的调试方法。它会在浏览器的**控制台**（Console）中输出信息：
```js
console.log("这段文字会出现在控制台");
```
就像医生用听诊器检查身体，开发者用 `console.log` 查看代码运行情况。
打开预览区，然后按 `F12` 打开开发者工具，切换到"Console"标签页，你就能看到输出了！
:::

:::example{title="看例子"}
下面的代码就是编辑器中你看到的。一个 `alert` 欢迎你，一个 `console.log` 输出信息：
```js
// alert()：弹出提示框，让页面和用户"说话"
alert("欢迎来到音乐编程之旅！");

// console.log()：在控制台输出信息，只有开发者能看到
console.log("页面加载完成，准备就绪！");
```
试试修改 `alert` 和 `console.log` 中的文字。
:::

:::task{title="动手试试 ✨"}
::::step{purpose="`alert()` 是 JS 最简单的交互方式——弹出一个提示框。体验 JavaScript 与 HTML/CSS 的不同：JS 能让页面做出反应。" expected="页面刷新后弹出了你自定义的欢迎语。这就是 JS——让页面与用户对话。"}
修改 `alert()` 中的文字，换成你自己的欢迎语
::::

::::step{purpose="`console.log()` 是开发者的耳朵——输出信息到控制台而不是弹窗。以后调试全靠它。" expected="控制台中显示了你修改后的文字。控制台只有你（开发者）能看到。"}
修改 `console.log()` 中的文字
::::

::::step{purpose="JavaScript 按从上到下的顺序执行。添加第二行，它就按顺序执行两条语句。" expected="控制台中出现两条输出，按你写的顺序排列。"}
试着添加第二行 `console.log()`，输出不同的内容
::::

::::step{purpose="控制台是 JavaScript 开发最重要的工具。以后每次调试代码，你都会用 F12 → Console。" expected="在 Console 面板中看到你的输出文字。如果看不到，检查是否切换到了 Console 标签页。"}
按 `F12` 打开浏览器控制台（Console 标签），看你的输出
::::

:::

:::recap
这一节你开始学习 JavaScript——让网页"活起来"的语言。`alert()` 弹出提示框跟用户对话，`console.log()` 在控制台输出信息（只有开发者能看到），你学会了按 F12 打开控制台来观察代码运行结果。HTML 决定了"有什么"，CSS 决定了"长什么样"，现在你有了第三种能力：JavaScript 决定了"能做什么"。
:::


