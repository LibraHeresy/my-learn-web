# 三段工作流 — HTML → CSS → JS 的正确顺序

:::music-analogy
作曲家从不一开始就写满整份总谱。通常先写旋律线（HTML），再配和声与配器（CSS），最后标注强弱与速度变化（JS）。写网页也一样——**永远先写结构，再写样式，最后写交互**。跳过任何一步都会让后面的工作变得混乱。
:::

:::explain{title="为什么顺序很重要？"}
在乐理篇中，你已经学会了三门"乐器"：
- **HTML**（HyperText Markup Language，超文本标记语言）— 页面的骨架
- **CSS**（Cascading Style Sheets，层叠样式表）— 页面的外观
- **JS / JavaScript** — 页面的"大脑"，让它能听你的指令做出反应
但从零开始构建一个页面时，**按什么顺序写**比写什么更重要。
标准的三段工作流：
**第一步：HTML 结构** — 把页面上所有内容先用标签写出来
- 标题、段落、图片、按钮……先全部摆上去
- 不关心颜色、大小、位置
- 就像先把音符写在五线谱上
**第二步：CSS 样式** — 给每个元素穿上合适的"衣服"
- 颜色、字体、间距、背景……
- 从外到内，从大到小
- 就像给旋律配上和声与音色
**第三步：JS（JavaScript）交互** — 让页面"动起来"
- 点击按钮会发生什么？
- 用户输入后如何响应？
- 就像指挥标注演奏法：这里渐强，那里渐慢
> 💡 **关于 JS 和 JavaScript**：JS 是 JavaScript 的缩写，它们是同一个东西。就像"贝多芬"有时被简称为"贝爷"——JS 就是 JavaScript 的昵称。
:::

:::explain{title="倒过来写会怎样？"}
新手常犯的错误是：一上来就写 CSS 或 JS，结果 HTML 结构老在变，前面写的样式和逻辑全乱了。
就像一个作曲家先写好配器再改旋律——每次改旋律都得重新配器，浪费时间。
**正确做法：先让 HTML 稳定下来，确认结构是对的，再动手写 CSS 和 JS。** 这就是乐理篇把三门语言分开教的原因——先熟悉每件"乐器"，现在合奏篇教你如何一起演奏它们。
:::

:::explain{title="两个常用的数学工具"}
在写 JS 交互时，经常需要"随机选一个"。JavaScript 内置了两个好用的数学方法：
**`Math.random()` — 生成随机数**
```js
Math.random(); // 返回 0 到 1 之间的随机小数，比如 0.3847、0.9215
```
每次调用都会返回不同的值，就像伸手到袋子里随机摸一颗糖。
**`Math.floor()` — 向下取整**
```js
Math.floor(3.8);  // 结果是 3
Math.floor(7.1);  // 结果是 7
Math.floor(0.95); // 结果是 0
```
把小数的小数部分直接切掉，只留整数部分。
**搭配使用 — 获取随机数组索引：**
```js
let arr = ["苹果", "香蕉", "橘子"];
let i = Math.floor(Math.random() * arr.length);
// Math.random()       → 0~1 之间的小数
// Math.random() * 3   → 0~3 之间的小数（如 1.2、2.8）
// Math.floor(...)     → 向下取整（如 1.2→1, 2.8→2）
// 最终 i 是 0、1、2 中的一个（三个索引）
arr[i]; // 随机选中的元素
```
这两个方法在后续课程中会经常用到！
:::

:::task{title="动手试试 ✨"}
::::step{purpose="从结构开始可以让你先确认页面上有哪些元素，就像作曲时先在五线谱上画出音符位置，之后再考虑音色和强弱。建立\"先骨架、后装饰\"的习惯，是前端开发最重要的纪律。" expected="预览区能看到标题、名言文字和一个按钮，但样式还很朴素——这很正常，下一步才是 CSS。"}
写 HTML 结构：创建一个 div 包含 h2 标题、p 名言、button 按钮
::::

::::step{purpose="CSS 是给 HTML 穿上\"衣服\"的过程。先确保内容可见，再美化外观，这样你能清楚地分辨\"结构问题\"和\"样式问题\"——避免同时调试两个维度。" expected="卡片有了温暖的米白背景和圆角边框，按钮变成暗红色并在鼠标悬停时变亮——页面看起来像一个真正的卡片了。"}
写 CSS 样式：给卡片加背景色（#FFFAF2）、圆角边框、内边距和居中排版；给按钮加暗红背景、白色文字、圆角和悬停变色效果
::::

::::step{purpose="JS 是页面的大脑，让静态卡片\"活\"起来。Math.random() + Math.floor() 的组合是后续大量交互的基础——随机选取、随机排序、随机展示都离不开这两个方法。" expected="点击\"换一句\"按钮后，名言文字随机变换成数组中的另一句，每次点击都有不同的结果——你的页面会响应了！"}
写 JS 交互：准备一个名言数组，用 querySelector 获取元素，给按钮绑定 click 事件，用 Math.random() 和 Math.floor() 随机选取名言并更新 textContent
::::

:::

:::recap
你学会了前端开发的三段工作流——先写 HTML 结构（把内容全部摆上去），再写 CSS 样式（美化外观），最后写 JS 交互（让页面动起来）。按这个顺序写，不会因为改结构而让样式和逻辑全乱了。
:::

:::listen-to
拉威尔《波莱罗》— 从一支长笛的简单旋律开始，逐步加入更多乐器，最后整个乐团爆发出磅礴的声响。这就是"先骨架、再装饰、后互动"的完美音乐示范。
:::

