万事开头难——但别担心，第一张卡片就像在五线谱上画出第一个音符。
先用 HTML 写出**一张**音乐收藏卡片，包含：
- 一首曲子的名称（用 `<h2>` 标题）
- 作曲家的名字（用 `<p>` 段落）
- 一张封面图片（用 `<img>` 标签）
- 音乐时期/风格分类（用 `<span>` 标签，如"巴洛克"）
把卡片包在一个 `<div>` 里，给它一个 class 名称，后面我们会用到。
