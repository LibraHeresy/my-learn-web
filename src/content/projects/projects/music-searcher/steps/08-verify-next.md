现在先按最小完成定义把项目收口，不用在这里展开一整套很长的验收模板。

**最小验收：**
- 输入关键词后能稳定拿到结果
- 防抖生效，请求不会跟着每次按键都触发
- loading、empty、error 这些状态都有明确反馈

**关键手测：**
1. 搜索 `贝多芬` 和 `Mozart`，确认结果正常返回
2. 搜索一个不存在的词，确认能进入 empty 状态
3. 在 Network 面板切到 `Offline` 再搜索，确认 error 状态成立

**如果卡住：**
- 先查输入值、请求 URL、响应结构和状态切换，再回到前面步骤逐段定位

**接下来怎么走：**
- 想继续原生应用路线：去 `music-collection-v1`
- 想把这套异步思维迁移到 Vue：去 `music-collection-v2`
