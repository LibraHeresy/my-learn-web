一张音乐家名片需要包含哪些信息？就像音乐会节目单上的作曲家介绍，至少要有：
- **名字** — 用 `<h1>` 大标题（如"路德维希·凡·贝多芬"）
- **简介** — 用 `<p>` 段落写一两句话（如"德国作曲家，古典与浪漫时期的桥梁"）
- **代表作** — 用 `<ul>` 无序列表列出 3-5 首著名作品
- **照片** — 用 `<img>` 标签放一张音乐家的照片
用 `<div>` 把所有内容包在一起，给 class="composer-card"。
先用你熟悉的标签把内容写出来，不用管好不好看——下一步我们会用 CSS 来美化。
